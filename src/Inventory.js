import { Typography } from 'antd';
import React, { useState } from 'react';

const { Title } = Typography;
function Inventory() {
   return (
      <div>
         <Title>Inventory test 3</Title>
      </div>
   );
}

export default Inventory;
